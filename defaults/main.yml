---

# set to 2 or 3 if required. leave as is to use the python Ansible uses
pip_version: null

pip_install_packages:
  - name: virtualenv
  - name: pyyaml
    state: present
  - name: awscli
    state: absent
  - name: requests
    virtualenv: /tmp/venv
  - name: pylint
    version: 1.9.4
