---

# set to 2 or 3 if required. leave as is to use the python Ansible uses
pip_version: null

pip_install_packages:
  - virtualenv
  - name: pyyaml
    state: present
  - name: awscli
    state: absent
  - name: requests
    virtualenv: /tmp/venv
  - name: jinja2
    version: 2.10
