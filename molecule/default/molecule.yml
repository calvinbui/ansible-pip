---
dependency:
  name: galaxy
driver:
  name: docker
lint:
  name: yamllint
platforms:
  - name: ansible-pip
    image: ubuntu:18.04
provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        pip_version: ${PIP_VERSION_NUMBER}
  lint:
    name: ansible-lint
scenario:
  name: default
verifier:
  name: testinfra
  lint:
    name: flake8
